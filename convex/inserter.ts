import { api } from "./_generated/api";
import { mutation } from "./_generated/server";

const NUM_TO_INSERT = 50;

export const insertRandom = mutation(async (ctx) => {
    for (let i = 0; i < NUM_TO_INSERT; i += 1) {
    }
});
